function climbingLeaderboard(ranked, player) {
  // Write your code here
  let leaderBoards = [];
  let boards = new ranked()
  for (let i = 0; i < player.length; i++) {
   
  }
  return boards;
}

console.log(climbingLeaderboard([100, 90, 90, 80], [70, 80, 100]));



let count = 0
for (let i = 0; i < s.length; i++) {
    let result = s.slice(i,i+m)

    let total = 0
    for (let j = 0; j < result.length; j++) {
        total += result[j];
      }
    if (total == d) {
        count += 1
    }
}
   return count
}